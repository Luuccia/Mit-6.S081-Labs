(1)0-a7存放参数，13放在了a2中

(2)没有这样的代码，编译器内联了这两个函数。汇编中只出现了<malloc><printf><exit>，而没有<f><g>，且从li a1,12可以看出来，编译器直接算出了结果，即进行了内联优化。

(3)0x630

(4)0x38，即函数的返回地址

(5)
57616=0xE110
0x00646c72小端存储则为72-6c-64-00
对照ASCII码表
72:r 6c:l 64:d 00:字符串结束标识
输出为：HE110 World

将i设置为0x726c4400
不需要，因为%x输出16进制，而57616的16进制数不会因端序而改变

(6)取决于调用函数之前寄存器a2中有什么

